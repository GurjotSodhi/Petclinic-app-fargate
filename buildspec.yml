version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk8
  pre_build:
    commands:
      - echo Nothing to do in prebuild
  build:
    commands:
      - echo Build started on `date`
      - mvn package
      - docker build . -t spring-petclinic
      - echo Build completed on `date`
  post_build:
    commands:
      - $(aws ecr get-login --region $AWS_REGION --no-include-email)
      - AWS_ACC=$(echo $CODEBUILD_BUILD_ARN | cut -d':' -f5)
      - REPOSITORY_URI=$AWS_ACC.dkr.ecr.$AWS_REGION.amazonaws.com
      - IMAGE_TAG=$(date +%Y%m%d%H%M)
      - aws ecr describe-repositories --registry-id ${AWS_ACC} --repository-name ${REPO} || aws ecr create-repository --repository-name ${REPO}
      - docker push $REPOSITORY_URI/${REPO}:latest
artifacts:
  files:
    - target/spring-petclinic-2.2.0.BUILD-SNAPSHOT.jar